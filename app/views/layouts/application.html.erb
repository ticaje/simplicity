<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= page_title %></title>

    <%= stylesheet_link_tag    "application", "simplicity/application" %>
    <%= javascript_include_tag "application", "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body class="<%= page_classes %>">
    <%= render 'layouts/simplicity/header' %>
    <%= render 'shared/flash_messages' %>
    <div class="page">
      <div id="main">
        <div class="row main-content">
          <%= yield %>
          <div class="sidebar">
            <%= yield(:sidebar) %>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
